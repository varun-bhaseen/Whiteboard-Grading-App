export class Teacher_Assignment {
    assignment_id: string
    teacher_id: string

    constructor(assignment_id, teacher_id) {
        this.assignment_id = assignment_id
        this.teacher_id = teacher_id
    }
}